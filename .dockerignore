################################################################################
# Docker build context exclusions
#
# Notes:
# - Use forward slashes (/) so patterns work consistently on Linux builders
#   (e.g., Railway).
# - Do NOT ignore `Apps/` because we want to ship import files inside the image.
################################################################################

# Build output
target/

# Logs / runtime artifacts
logs/
*.log
*.jsonl
*.gz
mcp-stdin.txt

# IDE / editor
.idea/
.vscode/
.cursor/
*.iml
*.ipr
*.iws
.settings/
.classpath
.project
.factorypath

# OS
.DS_Store
Thumbs.db

# Env files
.env
.env.local

# Fly config is irrelevant for Docker builds on Railway
fly.toml
